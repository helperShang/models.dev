# Ambient's OpenAPI schema exposes reasoning.effort. GLM-5.2 has two effective
# effort levels, high and max (sources accessed 2026-07-13).
# https://api.ambient.xyz/openapi.json
# https://docs.z.ai/guides/capabilities/thinking
name = "Ambient"
env = ["AMBIENT_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.ambient.xyz/v1"
doc = "https://ambient.xyz"
